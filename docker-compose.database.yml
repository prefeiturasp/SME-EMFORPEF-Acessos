version: "3.1"

volumes:
  postgres-data: {}

services:
  acessos-db:
    container_name: acessos-db
    restart: always
    image: postgres:11-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: postgres      
      POSTGRES_DB: acessos_db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - sme-network
  acessos-flyway:
    container_name: acessos-flyway
    restart: on-failure:5
    image: boxfuse/flyway:5.2.4
    command: -url=jdbc:postgresql://sme-db:5432/acessos_db?user=postgres&password=postgres -locations='filesystem:/opt/scripts' -outOfOrder=true migrate
    volumes:
      - ./scripts:/opt/scripts
    depends_on:
      - acessos-db
    networks:
      - sme-network
networks:
  sme-network:
    driver: bridge
    external:
      name: sme-network